String.prototype.daxie = function(){
    //首字母大写
    //console.log(this) //this代表字符串本身
    //截取瘦字母this.slice(0,1) 截取其他的 this.slice(1)
    //this.clice(0,1).toUpperCase()  this.slice(1).toLowerCase()
}